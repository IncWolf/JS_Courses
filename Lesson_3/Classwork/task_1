В первом случае, результат false так как объект равен лишь самому себе.
Во втором случае, логическая операция НЕ возле массива приведет его к логическому типу.
А сравнение массива с примитивом - это сравнение строки с соответствующим примитивом.
Так как пустой массив преобразуется в пустую строку, что в логическом эквиваленте равно "0", то 0 == 0 выводит true.